// 학번로그인 -> 회원가입 버튼 클릭하면 회원가입 페이지 화면으로 넘어간다.
import { View, Text, StyleSheet } from "react-native";

export default function TicketCreatScreen({navigation}) {
    return (
        <View>
            <Text>Hello</Text>
        </View>
    );
}

const styles = StyleSheet.create(
    {

    }
);